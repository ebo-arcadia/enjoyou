const initialState = {
    text: '', 
    movies: [] 
}

const MovieReducer = (state = initialState, action) => {
    switch(action.type) {
        default: 
            return state;
    }
}

export default MovieReducer
